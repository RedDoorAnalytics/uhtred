# uhtred

## Installation

The latest version of -uhtred- can be installed from this GitHub repository by typing the following in your Stata console:

```{stata}
net install uhtred, from("https://raw.githubusercontent.com/RedDoorAnalytics/uhtred/main/")
```
